# Copy to .env.local and set a strong password for the /admin area
ADMIN_PASSWORD=change-me

# Optional: secret used to sign customer login sessions. If unset, falls back to
# ADMIN_PASSWORD. Set a long random value in production.
SESSION_SECRET=change-me-too
